import React from 'react';
import Layout from './Layout.js';

const SamplePrivateRoute = ({ props }) => {
  return (
    <Layout>
      this is SamplePrivateRoute
    </Layout>
  );
};

export default SamplePrivateRoute;